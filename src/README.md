# Source

Production source only. No rejected prototype renderer is permitted here.

The codebase currently implements canon validation, deterministic game-state foundations, semantic Confluence generation, the 20-manifestation character contract, live world-state primitives, input/save boundaries, and a strict production-asset gate.

There is intentionally no placeholder visual renderer. A renderer is added only when individual production assets are ready to stage against Reference Zero.
