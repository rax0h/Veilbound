# Production assets only

`assets/` is intentionally empty of game art until real production assets exist.

Do not place reference boards, contact sheets, generated mockup screenshots, SVG stand-ins, primitive geometry exports, or temporary character/creature atlases here.

Each runtime asset must be one asset = one file and must appear in a production asset manifest with explicit reference lineage before the renderer may load it.
